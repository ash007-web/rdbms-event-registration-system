# Database credentials — copy this file to .env and fill in your values
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password_here
DB_NAME=event_registration_db

# Flask secret key (any random string is fine for local use)
SECRET_KEY=ers-lab-module5-secret
